# Environment & secrets — never commit these
.env
.env.*
*.env
secrets.json
secrets.yaml
secrets.yml
*_credentials.json
*_key.json
credentials/

# Claude Code — track config, but not secrets inside it
# .claude/ and .cloud/ are intentionally tracked
.claude/memory/        # auto-memory is local-only
.claude/local_settings.json

# Playwright MCP (local artefacts)
.playwright-mcp/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
dist/
build/
*.egg-info/
.pytest_cache/

# macOS
.DS_Store
