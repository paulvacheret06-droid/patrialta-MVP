# Environment & secrets — never commit these
.env
.env.*
*.env
secrets.json
secrets.yaml
secrets.yml
*_credentials.json
*_key.json
credentials/

# Claude Code — track config, but not secrets inside it
# .claude/settings.json and .cloud/settings.json are intentionally tracked
# .mcp.json is intentionally tracked (shared MCP server config)
.claude/memory/              # auto-memory is local-only
.claude/settings.local.json  # local overrides, not shared

# Playwright MCP (local artefacts)
.playwright-mcp/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
dist/
build/
*.egg-info/
.pytest_cache/

# macOS
.DS_Store
